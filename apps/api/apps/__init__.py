"""MurasFood domain modules.

Each subpackage is a Django application representing one bounded context.
Modules communicate through services and selectors — never by reaching into
another context's ORM internals.
"""
